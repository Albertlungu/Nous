#pragma once

namespace cfg {
    inline constexpr int vocab_size = 50257;
    inline constexpr int embedding_dim = 256;
    inline constexpr int max_seq_length = 256;
    inline constexpr int n = 10000;
    inline constexpr float dropout = 0.0f;
    inline constexpr int batch_size = 8;
}